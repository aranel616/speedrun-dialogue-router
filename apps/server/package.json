{
  "name": "@sdr/server",
  "version": "0.1.0",
  "private": true,
  "description": "Express API exposing the routing engine over HTTP",
  "main": "src/index.ts",
  "scripts": {
    "dev": "nodemon --exec ts-node --watch src --watch ../../packages --ext ts src/index.ts",
    "start": "ts-node src/index.ts"
  },
  "dependencies": {
    "@sdr/engine": "*",
    "@sdr/graph": "*",
    "@sdr/scripts": "*",
    "@sdr/shared": "*",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2"
  }
}
